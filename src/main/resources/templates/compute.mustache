<title>My application</title>
  <h1>Hello, This is Buğra's homework2!</h1>
   1st box:array of integer
   2nd box:an integer
   3rd box:array of strings (each line has one element)
   4th box:a string
   If number of integers(entered 2nd box) in integer array(entered 1st box) is equal to number of Strings(entered 4th box) in String array(entered 3rd box) returns true.Else return false 
   Result is {{result}}


  <form action="compute" method="post">
        <textarea name="input1" rows="10" cols="30"></textarea>
        <textarea name="input3" rows="10" cols="30"></textarea>        
        <textarea name="input2" rows="10" cols="30"></textarea>
        <textarea name="input4" rows="10" cols="30"></textarea>
 
       <br>
        <input type="submit">
  </form>

